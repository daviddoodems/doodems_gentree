{
  "name": "familia-arbol",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "deploy": "wrangler pages deploy . --project-name=familia-arbol",
    "db:schema": "wrangler d1 execute familia_arbol --remote --file=./schema.sql"
  },
  "devDependencies": {
    "wrangler": "^4.0.0"
  }
}
